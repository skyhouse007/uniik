Place your compressed `.glb` files here.

Example:

- `frontend/public/models/mattress.glb`

Tips:
- Keep polycount low (LOD if possible)
- Use Draco / meshopt compression when exporting
- Prefer KTX2/Basis texture compression for production

